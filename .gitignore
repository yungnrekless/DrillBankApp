node_modules/
*.tmp
.DS_Store
dist/

# Local harness for the localStorage tracker: a copy of a published chapter
# page, served from public/ so it gets a real http:// origin. Regenerated by
# hand from docs/, so it is a snapshot that goes stale on every republish.
public/lt-check.html
